'use client';

import React from 'react';

interface AdaptivePerformanceMonitorProps {
  onPerformanceChange?: (newTier: string) => void;
}

export default function AdaptivePerformanceMonitor({ onPerformanceChange }: AdaptivePerformanceMonitorProps) {
  return null;
}
